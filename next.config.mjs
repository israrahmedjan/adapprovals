/** @type {import('next').NextConfig} */
const nextConfig = {
  images: {
    domains: [
      "images.unsplash.com",
      "cdn-icons-png.flaticon.com",
      "absolute",
      "cdn.sanity.io",
      "via.placeholder.com",
    ],
  },
};

export default nextConfig;
